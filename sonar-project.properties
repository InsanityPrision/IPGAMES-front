sonar.projectKey=InsanityPrision_IPGAMES-front
sonar.organization=insanityprision

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=IPGAMES-front
#sonar.projectVersion=1.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=./src

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

sonar.javascript.lcov.reportPaths=./coverage/lcov.info

sonar.test.inclusion=**/*.test.tsx,**/*.test.ts

sonar.coverage.exclusions=**/*.test.tsx,src/main.tsx,**/*.test.ts,src/setUpTests.ts
